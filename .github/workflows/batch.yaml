name: 株価データ照会

on:
  schedule:
    - cron: '0 16 * * 1-5'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'adopt'
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: build with Gradle
        run: ./gradlew clean :build -x test
      - name: Set environment variables
        run: |
          echo "SLACK_WEBHOOK_URL=${{ secrets.SLACK_WEBHOOK_URL }}" >> $GITHUB_ENV
          echo "JQUANTS_EMAIL=${{ secrets.JQUANTS_EMAIL }}" >> $GITHUB_ENV
          echo "JQUANTS_PASSWORD=${{ secrets.JQUANTS_PASSWORD }}" >> $GITHUB_ENV
      - name: Run batch
        env:
         JQUANTS_EMAIL: ${{ secrets.JQUANTS_EMAIL }}
         JQUANTS_PASSWORD: ${{ secrets.JQUANTS_PASSWORD }}
        run: java -jar -Dspring.batch.job.name=tickerSlackJob -Dspring.profiles.active=github ./build/libs/stockAutomation-0.0.1-SNAPSHOT.jar stock=9449
      - name: Send Slack notification
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -X POST -H 'Content-type: application/json' --data '{"text":"Build completed successfully!"}' $SLACK_WEBHOOK_URL
